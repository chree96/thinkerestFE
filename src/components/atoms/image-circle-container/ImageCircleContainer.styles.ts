import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  imgStyle: {
    width: '100%',
    height: '100%',
    resizeMode: 'cover',
    borderRadius: 100,
  },
  circleContainer: {
    borderRadius: 100,
  },
});
